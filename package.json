{
  "name": "owen-web",
  "private": true,
  "version": "0.8.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:placeholders": "node check-placeholders.mjs",
    "test:experience": "playwright test",
    "verify": "npm run check:placeholders && npm run build && npm run test:experience"
  },
  "dependencies": {
    "gsap": "3.15.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vite": "^7.1.0"
  }
}
